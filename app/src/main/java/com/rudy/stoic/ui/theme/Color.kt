package com.rudy.stoic.ui.theme

import androidx.compose.ui.graphics.Color

// Stoic Launcher — Iron Man HUD Color Palette

// Primary — Cyan / Electric Blue
val CyanPrimary = Color(0xFF00E5FF)
val CyanGlow = Color(0x4D00E5FF)       // 30% opacity — for blur/shadow glow
val CyanBorder = Color(0x9900E5FF)     // 60% opacity — block borders
val CyanMuted = Color(0xFF80DEEA)      // muted cyan — secondary text

// Accent — Amber / Gold
val AmberAccent = Color(0xFFFFD740)

// Surfaces — Dark translucent
val RingBackground = Color(0xCC0A1929)  // 80% opacity
val BlockBackground = Color(0xD90D2137) // 85% opacity
val SurfaceDark = Color(0xFF0A1929)
val SurfaceDarkAlt = Color(0xFF0D2137)

// Text
val TextPrimary = Color(0xFFE0F7FA)     // light cyan-white
val TextSecondary = Color(0xFF80DEEA)   // muted cyan

// State
val StateActive = Color(0xFF00E5FF)     // ON state
val StateInactive = Color(0xFF37474F)   // OFF state

// Background
val BackgroundDark = Color(0xFF000000)
